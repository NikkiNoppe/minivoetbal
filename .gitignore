# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment (never commit secrets)
.env
.env.*
!.env.example

# Platform artifacts
.prewarm
.lovable/

# Supabase local CLI cache
supabase/.temp/

# Local / CI database dumps (never commit)
backups/

# Seizoens-JSON backups (financiële data — lokaal bewaren)
/archief/*
!/archief/.gitkeep

# Editor directories and files
.vscode/safari-debug-profile/
.vscode/chrome-debug-profile/
.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# CCE (code-context-engine)
# CCE local cache (per-machine, not for version control)
.cce/
# Claude Code local settings written by cce init
.claude/settings.local.json
# .mcp.json contains absolute paths regenerated by `cce init`
.mcp.json
